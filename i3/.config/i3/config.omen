# Host-specific configuration for omen

## Resize to ocuppy non broken part of the screen
bindsym $mod+u resize grow width 24 px or 24 ppt

## Set wallpaper
exec --no-startup-id sleep 1 && feh --bg-scale /usr/share/sunAndClouds.png

# Lock the system
# lock by blurring the screen:
bindsym $mod+l exec --no-startup-id ~/.config/i3/scripts/blur-lock
# lock in case after sleep
exec --no-startup-id xss-lock -l ~/.config/i3/scripts/blur-lock

# vim:filetype=i3config
